<?xml version="1.0" encoding="utf-8"?>
<!-- ⭐ API 31+ tem SplashScreen de plataforma: ela IGNORA o `windowBackground` e usa
     estes três atributos. Sem eles o relógio moderno mostraria o splash padrão (com
     a cor do tema), que foi o que o Play viu. O desenho é o MESMO da values/ — uma
     bola de 48dp centrada em preto —, para que a abertura seja idêntica em qualquer
     versão do Wear. -->
<resources>
    <style name="ScoreplaceWearSplash" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowSplashScreenBackground">@android:color/black</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
