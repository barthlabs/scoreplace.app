.DS_Store
.claude/
*.backup
*.bak
outputs/
functions/node_modules/
functions/.secret.local
extensions/

# E2E / dev tooling
node_modules/
playwright-report/
test-results/
.lighthouseci/
fix-null-participants.js
clear-match-results.js
admin-delete-phone-accounts.js
admin-delete-one-account.js
functions-autodraw/node_modules/

# Firebase CLI deploy cache
.firebase/
firebase-debug.log
firebase-debug*.log
# Emulador Firestore (testes de concorrência — npm run test:concurrency)
firestore-debug.log
firestore-debug*.log
ui-debug.log

# Artefatos/documentos locais de trabalho (não fazem parte do app)
AUDIT-codigo-morto.md
dicas-coachmarks-scoreplace.pdf
Custos do Projeto - scoreplace.app/

# WhatsApp VPS — .env real nunca commitado (o .env.vps.example sim)
infra/whatsapp/.env

# Planilha de cobertura de testes (local, nao versionar)
docs/testes-cobertura-*

# Capacitor — www/ é gerado por tools/build-www.js (fonte de verdade é a raiz)
www/
# Artefatos de build nativo (os projetos ios/ e android/ SÃO commitados; os builds não)
ios/App/Pods/
ios/App/App/public/
android/.gradle/
android/app/build/
android/build/
android/app/src/main/assets/public/
android/capacitor-cordova-android-plugins/
.gradle/

# Android signing keystore (segredo — nunca commitar)
android-signing/
android/keystore.properties
*.jks
*.keystore
torneio_bye_repescagem.xlsx

# Symlinks de node_modules (sem barra): `node_modules/` NÃO casa com um SYMLINK de mesmo
# nome — foi assim que dois symlinks apontando pra máquina local entraram no repo em
# 02/ago/2026 e CONGELARAM o GitHub Pages: o upload-pages-artifact roda
# `tar --dereference`, o link aponta pra nada no runner, o tar sai 1 e nada publica.
node_modules
functions/node_modules
functions-autodraw/node_modules
functions-stripe/node_modules
