'use strict'; const C=require('./profile-notifications-core'); let f=0,p=0; const ok=(n,v)=>{v?p++:(f++,console.error('✗ '+n));}; const bad=(n,v)=>{try{C.normalizeNotificationPreferences(v);ok(n,false);}catch(_){ok(n,true);}};
const x=C.normalizeNotificationPreferences({notifyEmail:false,notifyLevel:'importantes'}); ok('aceita canais e nível fechado',x.notifyEmail===false&&x.notifyLevel==='importantes'); bad('rejeita campo arbitrário',{phone:true}); bad('rejeita canal não booleano',{notifyEmail:'false'}); bad('rejeita nível desconhecido',{notifyLevel:'tudo'}); console.log((f?'❌':'✅')+' profile-notifications-core: '+p+' ok, '+f+' falharam');process.exit(f?1:0);
