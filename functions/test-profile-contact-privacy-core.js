'use strict';const C=require('./profile-contact-privacy-core');let f=0,p=0;const ok=(n,v)=>{v?p++:(f++,console.error('✗ '+n))},bad=(n,v)=>{try{C.normalizeContactPrivacy(v);ok(n,false)}catch(_){ok(n,true)}};const x=C.normalizeContactPrivacy({omitEmail:true,omitPhone:false});ok('aceita dois booleanos',x.omitEmail===true&&x.omitPhone===false);bad('rejeita campo extra',{omitEmail:true,email:'x'});bad('rejeita string',{omitPhone:'false'});console.log((f?'❌':'✅')+' profile-contact-privacy-core: '+p+' ok, '+f+' falharam');process.exit(f?1:0);
