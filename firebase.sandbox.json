{
  "//": "Config DEDICADA ao emulador dos testes de sandbox (FIX.SANDBOX.P2). NÃO usar pra deploy. Rodada via: npm run test:sandbox",
  "firestore": { "rules": "firestore.rules" },
  "functions": [{ "source": "functions", "codebase": "default" }],
  "emulators": {
    "firestore": { "port": 8093 },
    "auth": { "port": 9092 },
    "functions": { "port": 5011 },
    "ui": { "enabled": false },
    "singleProjectMode": true
  }
}
