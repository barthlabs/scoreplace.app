const fs=require('fs');const s=fs.readFileSync('js/views/tournaments-draw-prep.js','utf8');const a=s.indexOf('window._runVagasDraw = function'),b=s.indexOf('// v4.0.73:',a);let fail=0;function ok(v,m){if(v)console.log('✓ '+m);else{fail++;console.error('✗ '+m)}}const body=s.slice(a,b);ok(/_rosterSig/.test(body)&&/freshSig !== _rosterSig/.test(body),'aborta se elenco fresco mudou');ok(/ft\.drawSelectionDone/.test(body),'aborta seleção já aplicada');ok(!/saveTournament\(|AppStore\.sync\(/.test(body),'não grava snapshot inteiro');if(fail)process.exit(1);
