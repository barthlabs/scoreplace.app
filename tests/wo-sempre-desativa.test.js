'use strict';const {ok,base,run}=require('./liga-availability-test-kit');let s={pass:0,fail:0};console.log('──── W.O. sempre desativa ────');const t=base({participants:[{uid:'u-eliane',displayName:'Eliane',ligaActive:false,woDeactivatedAt:'x'}],standbyParticipants:[{uid:'u-vini',displayName:'Vini',ligaActive:true}],rounds:[{matches:[],monarchGroups:[]}]});run(t,'u-eliane',true);ok(!t.participants.some(p=>p.uid==='u-eliane'),'ao religar após W.O. sai do elenco',s);ok(t.standbyParticipants.map(p=>p.uid).join(',')==='u-vini,u-eliane','entra no fim da fila',s);const e=t.standbyParticipants[1];ok(e.ligaActive===true&&e.woSentToWaitlistAt&&!e.woDeactivatedAt,'estado W.O. acompanha a fila sem dupla marca',s);console.log(`✅ wo-sempre-desativa: ${s.pass} asserções, ${s.fail} falha(s)`);if(s.fail)process.exit(1);
