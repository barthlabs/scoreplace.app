'use strict';const {ok,base,run}=require('./liga-availability-test-kit');let s={pass:0,fail:0};console.log('──── W.O. volta e busca ────');let t=base({participants:[],standbyParticipants:[{uid:'u-a',displayName:'Ana',ligaActive:true}],rounds:[]});run(t,'u-a',true);ok(t.participants.some(p=>p.uid==='u-a')&&!t.standbyParticipants.length,'sem sorteio volta ao elenco para o próximo sorteio',s);t=base({participants:[],standbyParticipants:[{uid:'u-a',displayName:'Ana',ligaActive:true,woSentToWaitlistAt:'x'}],rounds:[{matches:[],monarchGroups:[]}]});run(t,'u-a',false);ok(t.participants.some(p=>p.uid==='u-a'&&p.ligaActive===false)&&!t.standbyParticipants.length,'desligar na fila volta ao elenco desativada',s);console.log(`  ${s.pass} ok, ${s.fail} falha(s)`);if(s.fail)process.exit(1);
