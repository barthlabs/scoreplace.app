'use strict';const fs=require('fs'),i=fs.readFileSync('functions-autodraw/index.js','utf8'),p=fs.readFileSync('js/views/tournaments-draw-prep.js','utf8');let f=0;const ok=(v,s)=>{console.log((v?'✓ ':'✗ ')+s);if(!v)f++;};ok(i.includes('decisions.closeEnrollment')&&i.includes("t.status='closed'"),'Function fecha inscrição dentro de drawRound');const a=p.slice(p.indexOf('window._grupos_f2Direct'),p.indexOf('// ============',p.indexOf('window._grupos_f2Direct')));ok(a.includes('_setDrawDecision')&&!a.includes('AppStore.mutate'),'atalho formatado só envia intenção');process.exit(f?1:0);
