'use strict';const {ok,base,run}=require('./liga-availability-test-kit');let s={pass:0,fail:0};console.log('──── inscrição pós-sorteio vai para espera ────');const t=base({participants:[{uid:'u-a',displayName:'Ana',ligaActive:false},{uid:'u-b',displayName:'Bia',ligaActive:true}],rounds:[{matches:[{id:'folga',isSitOut:true,sitOutReason:'inactive',p1Uid:'u-a'}],monarchGroups:[{playersUids:['u-b']}]}]});run(t,'u-a',true);ok(!t.participants.some(p=>p.uid==='u-a')&&t.standbyParticipants.some(p=>p.uid==='u-a'),'reativado sai dos inativos e entra na espera',s);ok(t.standbyParticipants[0].ligaActive===true,'espera registra a disponibilidade ativa',s);ok(!t.rounds[0].matches.some(m=>m.id==='folga'),'folga antiga sai com a reativação',s);console.log(`  ${s.pass} ok, ${s.fail} falha(s)`);if(s.fail)process.exit(1);
