'use strict';const fs=require('fs'),i=fs.readFileSync('functions-autodraw/index.js','utf8'),p=fs.readFileSync('js/views/tournaments-draw-prep.js','utf8');let f=0;const ok=(v,s)=>{console.log((v?'✓ ':'✗ ')+s);if(!v)f++;};ok(i.includes('decisions.groupConfig')&&i.includes("t.status='closed'"),'drawRound aplica configuração e fecha no documento fresco');const a=p.slice(p.indexOf('window._selectGroupsConfig'),p.indexOf('/* Seguir',p.indexOf('window._selectGroupsConfig')));ok(a.includes('_setDrawDecision')&&!a.includes('AppStore.mutate'),'painel só envia decisão ao sorteio');process.exit(f?1:0);
